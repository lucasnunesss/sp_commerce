

type WidthScreen = {
  innerWidth: string | number;

}

type bgSection = {
  bg: string;
  subTitle: string;
  btn: boolean;
  children: React.ReactNode;
}

type Product = {
  _id: string;
  img: string;
  title: string;
  price: number;
  qtd: number;
};
